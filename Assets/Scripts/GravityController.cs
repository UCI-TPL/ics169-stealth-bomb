using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GravityController : MonoBehaviour {

    //This script was made to a Custom Inspector could be used to edit gravity 
    //Later this can be added to GameController but this was made seperate to avoid conflicts

    public float gravity;

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
